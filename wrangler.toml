# Cloudflare Pages 配置
# 注意：Pages 项目通常不需要 wrangler.toml，这个文件仅供参考
name = "c-game"
compatibility_date = "2024-01-01"

# Pages 使用 pages_build_output_dir 而不是 site.bucket
pages_build_output_dir = "."

# 不要使用 [site] 配置，那是给 Workers Sites 用的
# 静态网站不需要 main 字段
